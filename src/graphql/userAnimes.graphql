query($user: String) {
  collection: MediaListCollection(userName: $user, type:ANIME, status:COMPLETED) {
    lists {
      name
      entries {
        mediaId
        score
        media {
          coverImage {
            medium
            large
          }
          title {
            userPreferred
          }
        }
      }
    }
  }
}
